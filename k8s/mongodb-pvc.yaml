apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mongodb-claim

spec:
  accessModes:
    - ReadWriteOnce

  volumeMode: Filesystem

  resources:
    requests:
      storage: 1Gi

  storageClassName: standard
